// I think that the way to use this one is to import it, not assign it to a var.
// I'm not SURE, but I THINK this should let me send things to the console from any script.
// Which means it runs on every tick.

var _log = []
var active = 0
var _style = -2

var i = 0

?loc.begin
  var p = ui.AddPanel()
  p.visible=true
  p.color = #red
  p.anchor = top_right
  p.dock = top_right
  p.y = 1
  p.w = 32
  p.h = 12
  p.style = _style
  newLine("Hello World", 0)

func newLine(str, offset)
  // given a string, create the text element, size/align it to the parent panel p, and place it there correctly.
  var t = ui.AddText()
  t.text = str
  t.w = p.w
  t.align = left
  t.dock = top_left
  t.anchor = top_left
  t.x = 1
  t.y = 1 + offset
  p.Add(t)

//func log(str)




?key=bumpRBegin // C to toggle the console display on or off
  ?p.visible=true
    p.visible=false
  :
    p.visible=true

/*
// Cycles the panel style. Disabled to free up the key.
// Maybe I can put it onto Shift-C?
?key=backBegin
  _style++
  ?_style>8
    _style=-8 
  p.style=_style
*/
